# osu! scout web

Next.js prototype for the osu! tournament power leaderboard.

## Run locally

Start the FastAPI backend from the repo root:

```powershell
python api.py
```

Then start the frontend:

```powershell
cd web
npm.cmd install
npm.cmd run dev
```

Open `http://localhost:3000`.
